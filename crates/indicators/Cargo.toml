[package]
name = "indicators"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.148"
sqlx = { version = "0.8.6", features = ["bigdecimal"]}

# My modules
bars = { path = "../bars" }

